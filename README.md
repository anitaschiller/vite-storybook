# Vite with storybook

To run the project:

1. `$ npm install`
2. `$ npm run storybook`

**Exercise 1:**
Can be found in `components/Card.jsx` - "Card with an Author"

Hint: If you get an error message
